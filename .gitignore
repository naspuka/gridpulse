# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Env / secrets
.env
.env.*
!.env.example

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
plan.bin
# (we DO commit .terraform.lock.hcl — provider version lock)

# Dagster local state
.dagster_home/
dagster_home/

# DuckDB
*.duckdb
*.duckdb.wal

# dbt — keep project + models + tests + macros + .yml; ignore artefacts.
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/profiles.yml

# IDE
.idea/
.vscode/
